import { Discover, Chat, User } from "../screens";

export const routeConfig = {
  Discover: {
    screen: Discover
  },
  Chat: {
    screen: Chat
  },
  User: {
    screen: User
  }
};

export default routeConfig;
